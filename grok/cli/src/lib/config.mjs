// Shared defaults for grok CLI (search + automations).

export const DEFAULT_CDP_PORT = 9221;
export const DEFAULT_CDP = `http://127.0.0.1:${DEFAULT_CDP_PORT}`;
export const GROK_BASE = 'https://grok.com';
export const DEFAULT_TIMEOUT_MS = 120_000;
export const DEFAULT_OUT_DIR = 'downloads';
